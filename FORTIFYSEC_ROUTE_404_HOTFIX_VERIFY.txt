FORTIFYSEC ROUTE 404 HOTFIX

Evidence before patch:
- Vercel latest deployment compiled /ctf, /painel/ctf, /empresa and /empresa/login.
- Public /ctf returned HTTP 200 on both deployment URL and www.fortifysec.com.br.
- Production runtime log recorded GET /empresa -> 404 after employer login.
- Existing admin navigation already used native anchors explicitly to prevent stale App Router state after deploys.

Patch:
- DashboardNav now uses native anchors for all core internal navigation.
- SiteHeader uses native anchors for navigation and hard navigation after logout.
- Company login uses POST /api/company/login and a normal HTTP 303 redirect to /empresa.
- New app/api/company/login/route.ts validates credentials and active company membership server-side.
- No CSS, database schema, Azure provider, Labs, CTF schema or visual design changed.
