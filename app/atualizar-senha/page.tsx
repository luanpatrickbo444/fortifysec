import { updatePasswordAction } from '@/app/actions'
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="auth-simple"><form className="login-card" action={updatePasswordAction}><span className="section-index">PASSWORD UPDATE</span><h2>Definir nova senha</h2><p>Use no mínimo 8 caracteres e evite reutilizar senhas corporativas antigas.</p>{q.error&&<div className="form-error">Não foi possível atualizar. Verifique a sessão e a nova senha.</div>}<label>Nova senha<input type="password" name="password" minLength={8} required autoComplete="new-password"/></label><button className="btn full">ATUALIZAR SENHA →</button></form></main>}
