import Link from 'next/link'
import { requestPasswordResetAction } from '@/app/actions'
export default async function Page({searchParams}:{searchParams:Promise<{ok?:string,error?:string}>}){const q=await searchParams;return <main className="auth-simple"><form className="login-card" action={requestPasswordResetAction}><Link href="/" className="brand"><span className="brand-bracket">[</span>FORTIFY<span>SEC</span><span className="brand-bracket">]</span><small>CLOUD</small></Link><span className="section-index">PASSWORD RECOVERY</span><h2>Recuperar acesso</h2><p>Informe seu e-mail corporativo para receber o link de redefinição.</p>{q.ok&&<div className="form-success">Se o e-mail estiver cadastrado, enviaremos as instruções.</div>}{q.error&&<div className="form-error">Não foi possível processar a solicitação.</div>}<label>E-mail<input type="email" name="email" required/></label><button className="btn full">ENVIAR LINK →</button><p className="login-help"><Link href="/acesso">Voltar ao portal</Link></p></form></main>}
