"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {browserSupabase} from "@/lib/supabase";
export default function RecoverPage(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[error,setError]=useState(false);const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setMessage("");try{const{error:reason}=await browserSupabase().auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:`${location.origin}/redefinir-senha`});if(reason)throw reason;setError(false);setMessage("Se o e-mail estiver cadastrado, enviaremos as instruções de recuperação.")}catch(reason){setError(true);setMessage(reason instanceof Error?reason.message:"Não foi possível enviar o link.")}finally{setLoading(false)}}return <main className="portal-root auth-layout"><section className="auth-panel"><Link className="portal-logo" href="/"><span className="portal-logo-mark">F/</span> FORTIFYSEC</Link><div className="auth-panel-main"><span className="portal-label">{"// RECUPERAÇÃO DE ACESSO"}</span><h1 className="auth-title">RECUPERE<br/>SUA SENHA.</h1><p className="auth-description">Informe o e-mail cadastrado. Você receberá um link seguro para definir uma nova senha.</p><form className="auth-form" onSubmit={submit}><label className="portal-field"><span>E-MAIL DA CONTA</span><input type="email" value={email} onChange={event=>setEmail(event.target.value)} required autoComplete="email" placeholder="voce@email.com"/></label><button className="portal-button" disabled={loading}>{loading?"ENVIANDO...":"ENVIAR LINK DE RECUPERAÇÃO →"}</button></form>{message&&<p className={`portal-alert ${error?"error":""}`} role="status">{message}</p>}<div className="auth-back"><Link className="portal-text-button" href="/login">← VOLTAR PARA O LOGIN</Link></div></div></section><aside className="auth-visual" aria-hidden="true"><div className="auth-radar"><div className="auth-shield">F/</div></div></aside></main>}
