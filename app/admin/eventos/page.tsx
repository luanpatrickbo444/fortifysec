import { PortalHeader } from '@/components/PortalHeader';import { StatusPill } from '@/components/StatusPill';import { getAdminEvents } from '@/lib/admin-data';
export default async function Page(){const events=await getAdminEvents();return <><PortalHeader kicker="ADMIN / INGEST" title="Eventos de integrações" copy="Eventos recebidos dos provedores/ferramentas de backup via API."/><div className="portal-card"><div className="data-table team-table"><div className="table-row head"><span>Horário</span><span>Provedor</span><span>Tipo</span><span>Status</span></div>{events.map((e:any)=><div className="table-row" key={e.id}><span>{new Date(e.received_at).toLocaleString('pt-BR')}</span><strong>{e.provider}</strong><span>{e.event_type}</span><StatusPill>RECEBIDO</StatusPill></div>)}</div></div></>}
