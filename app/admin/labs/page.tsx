import { Boxes, Eye, EyeOff, PlusCircle } from 'lucide-react'
import { DashboardShell } from '@/components/DashboardShell'
import { DifficultyMeter } from '@/components/ui/DifficultyMeter'
import { SubmitButton } from '@/components/ui/SubmitButton'
import { requireAdmin } from '@/lib/auth'
import { adminCreateLabAction, adminToggleLabPublishedAction } from '@/app/actions'

export default async function AdminLabs(){
 const {supabase}=await requireAdmin();const {data:labs}=await supabase.from('labs').select('id,title,slug,description,difficulty,estimated_minutes,tags,published,provider_lab_id,connection_url').order('created_at',{ascending:false})
 return <DashboardShell admin><div className="page-head internal-page-head"><div><div className="kicker">ADMIN / CYBER RANGE</div><h1>Cyber Labs</h1><p>Cadastre máquinas, ambientes e disponibilidade do range.</p></div><span className="pill active"><Boxes size={13}/>{(labs||[]).length} LABS</span></div><div className="admin-studio-grid"><form action={adminCreateLabAction} className="card content-studio"><div className="studio-header"><div><span className="section-index">NEW LAB</span><h2>Novo laboratório</h2><p>Defina a experiência, dificuldade e infraestrutura.</p></div><PlusCircle size={27}/></div><div className="two-col"><div className="field"><label>Título</label><input required name="title"/></div><div className="field"><label>Slug</label><input required name="slug"/></div><div className="field"><label>Dificuldade</label><select name="difficulty"><option>Easy</option><option>Medium</option><option>Hard</option><option>Insane</option></select></div><div className="field"><label>Sessão (min)</label><input name="estimated_minutes" type="number" min="15" defaultValue="60"/></div><div className="field"><label>Tags</label><input name="tags" placeholder="linux, web, privesc"/></div><div className="field"><label>Provider Lab ID</label><input name="provider_lab_id"/></div></div><div className="field"><label>Endpoint / VPN fixo</label><input name="connection_url"/></div><div className="field"><label>Descrição</label><textarea name="description" rows={4}/></div><div className="field"><label>Instruções</label><textarea name="instructions" rows={6}/></div><SubmitButton idleLabel="PUBLICAR LAB →" pendingLabel="PUBLICANDO LAB..."/></form><aside className="card studio-guide"><span className="section-index">RANGE CHECK</span><h3>Antes de publicar</h3><div className="guide-step"><strong>01</strong><div><b>Infra pronta</b><span>Use provider ID ou endpoint de fallback.</span></div></div><div className="guide-step"><strong>02</strong><div><b>TTL coerente</b><span>Tempo deve permitir concluir a missão.</span></div></div><div className="guide-step"><strong>03</strong><div><b>Briefing claro</b><span>Explique o objetivo sem entregar a solução.</span></div></div></aside></div><section className="admin-resource-grid">{(labs||[]).map((l:any)=><article className="card admin-resource-card" key={l.id}><div className="panel-head"><span className={`pill ${l.published?'active':'locked'}`}>{l.published?<><Eye size={11}/> PUBLICADO</>:<><EyeOff size={11}/> RASCUNHO</>}</span><DifficultyMeter difficulty={l.difficulty}/></div><h3>{l.title}</h3><p>{l.description||'Sem descrição.'}</p><div className="meta-row"><span>{l.estimated_minutes} MIN</span><span>{l.provider_lab_id?'PROVIDER':'FALLBACK'}</span><span>{(l.tags||[]).slice(0,2).join(' · ')||'SEM TAG'}</span></div><form action={adminToggleLabPublishedAction}><input type="hidden" name="lab_id" value={l.id}/><input type="hidden" name="published" value={String(l.published)}/><SubmitButton className="btn secondary" idleLabel={l.published?'RETIRAR LAB':'PUBLICAR LAB'} pendingLabel="ATUALIZANDO..."/></form></article>)}</section></DashboardShell>
}
