import { DashboardShell } from '@/components/DashboardShell'
import { requireAdmin } from '@/lib/auth'
import { adminSetEnrollmentAction } from '@/app/actions'
export default async function AdminEnrollments(){const {supabase}=await requireAdmin();const [{data:users},{data:courses},{data:enrollments}]=await Promise.all([supabase.from('profiles').select('id,name,email').eq('role','student').order('name'),supabase.from('courses').select('id,title').order('title'),supabase.from('enrollments').select('id,user_id,course_id,status,source,created_at,profiles(name,email),courses(title)').order('created_at',{ascending:false})]);return <DashboardShell admin><div className="page-head"><div><div className="kicker">ADMIN / ENROLLMENTS</div><h1>Matrículas</h1><p>Gerencie acessos aos cursos e acompanhe o status de cada aluno.</p></div></div><form action={adminSetEnrollmentAction} className="card" style={{marginBottom:18}}><div className="two-col"><div className="field"><label>Aluno</label><select name="user_id" required><option value="">Selecione</option>{(users||[]).map((u:any)=><option key={u.id} value={u.id}>{u.name} — {u.email}</option>)}</select></div><div className="field"><label>Curso</label><select name="course_id" required><option value="">Selecione</option>{(courses||[]).map((c:any)=><option key={c.id} value={c.id}>{c.title}</option>)}</select></div><div className="field"><label>Status</label><select name="status"><option value="active">Ativa</option><option value="pending">Pendente</option><option value="blocked">Bloqueada</option><option value="expired">Expirada</option></select></div></div><button className="btn">SALVAR MATRÍCULA</button></form><div className="tablewrap"><table><thead><tr><th>Aluno</th><th>Curso</th><th>Status</th><th>Origem</th><th>Criada</th></tr></thead><tbody>{(enrollments||[]).map((e:any)=><tr key={e.id}><td>{e.profiles?.name||e.profiles?.email}</td><td>{e.courses?.title}</td><td><span className={`pill ${e.status==='active'?'active':e.status==='blocked'?'danger':'locked'}`}>{e.status}</span></td><td className="mono">{e.source}</td><td>{new Date(e.created_at).toLocaleDateString('pt-BR')}</td></tr>)}</tbody></table></div></DashboardShell>}
