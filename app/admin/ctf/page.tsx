import { DashboardShell } from '@/components/DashboardShell'
import { requireAdmin } from '@/lib/auth'
import { adminCreateCtfAction } from '@/app/actions'
export default async function AdminCtf(){const {supabase}=await requireAdmin();const {data:events}=await supabase.from('ctf_events').select('*').order('starts_at',{ascending:false});return <DashboardShell admin><div className="page-head"><div><div className="kicker">ADMIN / CTF</div><h1>Eventos CTF</h1><p>Publique competição, janela de execução e premiação.</p></div></div><form action={adminCreateCtfAction} className="card" style={{marginBottom:18}}><div className="two-col"><div className="field"><label>Título</label><input name="title" required/></div><div className="field"><label>Prêmio</label><input name="prize_text" placeholder="R$ 15.000"/></div><div className="field"><label>Início</label><input name="starts_at" type="datetime-local" required/></div><div className="field"><label>Fim</label><input name="ends_at" type="datetime-local" required/></div><div className="field"><label>Status</label><select name="status"><option value="scheduled">Agendado</option><option value="live">Ao vivo</option><option value="finished">Finalizado</option></select></div></div><div className="field"><label>Descrição</label><textarea name="description" rows={4}/></div><button className="btn">PUBLICAR CTF</button></form><div className="tablewrap"><table><thead><tr><th>Evento</th><th>Início</th><th>Fim</th><th>Prêmio</th><th>Status</th></tr></thead><tbody>{(events||[]).map((e:any)=><tr key={e.id}><td>{e.title}</td><td>{new Date(e.starts_at).toLocaleString('pt-BR')}</td><td>{new Date(e.ends_at).toLocaleString('pt-BR')}</td><td>{e.prize_text||'—'}</td><td><span className={`pill ${e.status==='live'?'active':''}`}>{e.status}</span></td></tr>)}</tbody></table></div></DashboardShell>}
