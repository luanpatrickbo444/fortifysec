import { DashboardShell } from '@/components/DashboardShell'
import { requireAdmin } from '@/lib/auth'
import { adminCreateCourseAction, adminToggleCourseAction } from '@/app/actions'
export default async function AdminCourses(){const {supabase}=await requireAdmin();const {data:courses}=await supabase.from('courses').select('id,title,slug,price_cents,published').order('created_at',{ascending:false});return <DashboardShell admin><div className="page-head"><div><div className="kicker">ADMIN / ACADEMY</div><h1>Cursos</h1><p>Crie, publique e retire cursos do catálogo sem liberar matrícula automaticamente.</p></div></div><form action={adminCreateCourseAction} className="card" style={{marginBottom:18}}><div className="two-col"><div className="field"><label>Título</label><input required name="title"/></div><div className="field"><label>Slug</label><input required name="slug"/></div><div className="field"><label>Preço (R$)</label><input name="price" type="number" step="0.01" defaultValue="2997.00"/></div></div><div className="field"><label>Descrição</label><textarea name="description" rows={4}/></div><button className="btn">CRIAR CURSO</button></form><div className="tablewrap"><table><thead><tr><th>Curso</th><th>Slug</th><th>Preço</th><th>Status</th><th>Ação</th></tr></thead><tbody>{(courses||[]).map((c:any)=><tr key={c.id}><td>{c.title}</td><td className="mono">{c.slug}</td><td>R$ {(c.price_cents/100).toFixed(2).replace('.',',')}</td><td><span className={`pill ${c.published?'active':'locked'}`}>{c.published?'Publicado':'Rascunho'}</span></td><td><form action={adminToggleCourseAction}><input type="hidden" name="course_id" value={c.id}/><input type="hidden" name="published" value={String(c.published)}/><button className="btn secondary small">{c.published?'RETIRAR':'PUBLICAR'}</button></form></td></tr>)}</tbody></table></div></DashboardShell>}
