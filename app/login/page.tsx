import Link from 'next/link'
import { loginAction } from '@/app/actions'
export default async function Login({searchParams}:{searchParams:Promise<{erro?:string,sucesso?:string}>}){const p=await searchParams;return <div className="authwrap"><div className="authcard"><div className="eyebrow">Acesso seguro</div><h1>Entrar na FortifySec</h1><p className="muted">Use sua conta de aluno ou administrador.</p>{p.erro&&<p className="error">{p.erro}</p>}{p.sucesso&&<p className="success">{p.sucesso}</p>}<form action={loginAction}><div className="field"><label>E-mail</label><input required type="email" name="email" autoComplete="email"/></div><div className="field"><label>Senha</label><input required type="password" name="password" autoComplete="current-password"/></div><button className="btn" style={{width:'100%'}}>Entrar</button></form><p className="muted"><Link href="/recuperar-senha">Esqueci minha senha</Link> · <Link href="/cadastro">Criar conta</Link></p></div></div>}
