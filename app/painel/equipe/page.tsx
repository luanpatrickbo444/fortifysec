import { PortalHeader } from '@/components/PortalHeader';import { StatusPill } from '@/components/StatusPill';import { getMembersData } from '@/lib/portal-data';
export default async function Page(){const members=await getMembersData();return <><PortalHeader kicker="ACCESS / RBAC" title="Equipe e acessos" copy="Quem pode visualizar, operar ou solicitar recuperação."/><div className="portal-card"><div className="data-table team-table"><div className="table-row head"><span>Nome</span><span>Área</span><span>Perfil</span><span>Status</span></div>{members.length?members.map((m:any)=><div className="table-row" key={m.name}><strong>{String(m.name)}</strong><span>{String(m.area)}</span><span>{String(m.role)}</span><StatusPill>ATIVO</StatusPill></div>):<div className="table-empty">Nenhum membro vinculado.</div>}</div></div></>}
