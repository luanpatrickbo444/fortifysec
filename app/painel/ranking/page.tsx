import { DashboardShell } from '@/components/DashboardShell'
import { requireUser } from '@/lib/auth'
export default async function Ranking(){const {supabase,user}=await requireUser();const [{data:p},{data:ranking}]=await Promise.all([supabase.from('profiles').select('role').eq('id',user.id).single(),supabase.rpc('get_leaderboard',{limit_count:100})]);return <DashboardShell admin={p?.role==='admin'}><div className="page-head"><div><div className="kicker">COMMUNITY / LEADERBOARD</div><h1>Ranking técnico</h1><p>XP vem de aulas e challenges validados pela plataforma.</p></div></div><div className="tablewrap"><table><thead><tr><th>Posição</th><th>Operador</th><th>Headline</th><th>XP</th></tr></thead><tbody>{(ranking||[]).map((r:any,i:number)=><tr key={r.id}><td className="rank">#{String(i+1).padStart(2,'0')}</td><td><strong>{r.name}</strong></td><td>{r.headline||'—'}</td><td><span className="pill active">{r.xp} XP</span></td></tr>)}</tbody></table></div></DashboardShell>}
